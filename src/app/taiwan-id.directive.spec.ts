import { TaiwanIdDirective } from './taiwan-id.directive';

describe('TaiwanIdDirective', () => {
  it('should create an instance', () => {
    const directive = new TaiwanIdDirective();
    expect(directive).toBeTruthy();
  });
});
